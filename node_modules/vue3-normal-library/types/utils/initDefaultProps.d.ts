import { PropType } from "vue";
import { VueTypeValidableDef, VueTypeDef } from "vue-types";
export declare function initDefaultProps<T>(types: T, defaultProps: {
    [K in keyof T]?: T[K] extends VueTypeValidableDef<infer U> ? U : T[K] extends VueTypeDef<infer U> ? U : T[K] extends {
        type: PropType<infer U>;
    } ? U : any;
}): T;
